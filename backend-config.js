/**
 * backend-config.js — Trade Guardian backend URL configuration.
 *
 * Set TG_BACKEND_URL to the root of your deployed API server (no trailing slash).
 * This is read by live-connect.js to establish the Socket.IO connection and
 * to fetch initial account/risk/position data via REST.
 *
 * Development (Replit):
 *   The URL below is your Replit dev domain. It is stable for this workspace.
 *
 * Production (GitHub Pages → Replit Deployment):
 *   Replace this with your Replit deployment URL when you publish the backend,
 *   e.g. "https://your-repl-name.replit.app"
 */
window.TG_BACKEND_URL = "https://8e68e84a-2854-421b-a86c-098ebd02199f-00-1uhxsih30yejk.kirk.replit.dev";
